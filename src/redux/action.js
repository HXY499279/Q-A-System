export const questionId = (value) => (
    {
        type:'saveID',
        value
    }
)
export const questionChangeTitle = (value) => (
    {
        type:'changeTitle',
        value
    }
)
export const questionChangeDescribe = (value) => (
    {
        type:'changeDescribe',
        value
    }
)
export const questionChangeImg = (value) => (
    {
        type:'changeImg',
        value
    }
)
export const stuId = (value) => (
    {
        type:'changeImg',
        value
    }
)

